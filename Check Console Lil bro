
local function executeCommand()
    -- Replace the following line with your desired command
    print("Real people join our telegram t.me/Lokerz or you ded🔥")
end

-- Function to start the repeating task
local function startRepeatingTask(interval)
    while true do
        executeCommand()
        wait(interval)
    end
end

startRepeatingTask(5)
